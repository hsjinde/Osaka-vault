# Core Rules

> **⚠️ 優先閱讀**：所有協作者與 AI 應先閱讀本檔，了解 Vault 結構與工作規則。

本 Vault 為 **大阪旅遊知識庫**：AI 從原始資料（網頁剪藏、文章）提煉資訊進 `wiki/` 知識層，原始資料保持唯讀。

## 🛠 工具與協作

- AI 優先讀寫 `wiki/` 目錄下的檔案
- `原始資料/` 為 Obsidian 插件匯入的剪藏，AI 唯讀不修改
- 跨檔修改前先確認影響範圍，避免覆蓋既有筆記

## 📁 目錄與知識架構

```
大阪-vault/
├── core_rules.md         ← 本檔，AI 工作規則（優先閱讀）
├── AGENTS.md             ← Agent 補充設定
├── 原始資料/             ← Obsidian 插件匯入（網頁剪藏、文章），唯讀
│   ├── CHAT/             ← AI 對話記錄暫存（用戶要求時建立）
│   ├── 景點/
│   ├── 餐廳/
│   ├── 交通/
│   └── 別人行程/          ← 別人的行程、Google Maps 清單、他人攻略
├── Osaka Trip/           ← 已確認行程（機加酒、訂單等）
├── wiki/                 ← AI 維護的知識層
│   ├── index.md          ← 索引頁（查詢時先讀此定位）
│   ├── log.md            ← 操作日誌（只附加）
│   ├── entities/         ← 實體頁
│   │   ├── 景點/         → 大阪城.md、環球影城.md、道頓堀.md
│   │   ├── 餐廳/         → 個別餐廳.md
│   │   ├── 交通/         → 關西機場.md、大阪地鐵.md、周遊券.md
│   │   ├── 住宿/         → 個別飯店.md
│   │   ├── 區域/         → 難波.md、梅田.md、心齋橋.md、天王寺.md
│   │   └── 購物/         → 個別商場/店鋪.md
│   ├── concepts/         ← 概念／攻略頁（從原始資料提煉）
│   │   ├── 行程規劃.md
│   │   ├── 美食指南.md
│   │   ├── 購物指南.md
│   │   └── 晉德的大阪行推薦.md  ← source_type: reference（參考用）
│   └── queries/          ← 有價值的查詢結果歸檔
└── .obsidian/
```

### 📌 Wiki 分區說明

| 分區 | 說明 | 內容 |
|------|------|------|
| **已確認行程** | 已付款、確定的行程 | `Osaka Trip/` 內的檔案 |
| **我的規劃** | AI 協助生成的規劃草稿 | `wiki/concepts/` 中標注為「規劃」的頁面 |
| **參考資料** | 別人的行程/攻略/清單 | `wiki/concepts/` 中標注為「參考」的頁面，如「晉德的大阪行推薦」 |

> ⚠️ **重要**：參考資料僅供日後規劃參考，不可直接視為「我的行程」。

## 📥 Clippings 整理規則

`Clippings/` 為暫存區，AI 整理時依內容主題移至 `原始資料/` 對應子目錄，移動後刪除原檔。

### 分類標準

| 內容類型              | 目標路徑         |
| ----------------- | ------------ |
| 餐廳、美食影片、超商清單、食記   | `原始資料/餐廳/`   |
| 景點、主題樂園、VLOG、體驗活動 | `原始資料/景點/`   |
| 票券、交通攻略、機場路線      | `原始資料/交通/`   |
| 別人的行程、Google Maps 清單、他人攻略 | `原始資料/別人行程/` |
| AI 對話記錄              | `原始資料/CHAT/`   |
| 綜合型（橫跨多類別）        | `原始資料/`（根目錄） |

### 整理流程

1. 讀取 `Clippings/` 內所有檔案
2. 依上方分類標準判斷目標路徑
3. 將檔案**移動**（非複製）至對應子目錄
4. 若目標已有同名檔案，加日期後綴避免覆蓋
5. 整理完成後在 `wiki/log.md` 追加一筆操作記錄

---

## 🔑 觸發關鍵字

當使用者提及以下關鍵字時對應處理：

- **整理這篇 / 消化 / 提取** → 讀取原始資料，提取資訊建立或更新 wiki 實體頁
- **規劃行程 / 安排路線** → 讀取 wiki 中相關實體，生成行程建議（含每日行程框架，見下方「行程起草」節）
- **起草每日行程 / 幫我排 D1~D5** → 依 `Osaka Trip/` 確認資訊 + wiki 知識，生成 D1–D5 時間軸框架，存入 `Osaka Trip/itinerary-draft.md`（若已有則更新）
- **查詢 / 找 / 推薦** → 從 wiki 搜尋資訊回覆
- **健康檢查 / lint** → 檢查 wiki 結構完整性（孤立頁面、缺連結、過期資訊）
- **存查詢 / 歸檔** → 將有價值的問答存進 `wiki/queries/`

## ✍️ 實體頁格式規範

實體頁格式參考 `wiki/entities/` 下的筆記：

```yaml
---
title: 大阪城
tags: [景點, 大阪]
updated: 2026-06-08
source_count: 2
source_type: entity  # entity | plan | reference
---
```

- **frontmatter**：title、tags、updated、source_count（必要），可依類型加 rating、budget 等
- **source_type**：
  - `entity`：實體頁（景點、餐廳、交通等）— 預設值，可省略
  - `plan`：我的規劃（AI 協助生成的行程草稿）
  - `reference`：參考資料（別人的行程/攻略/清單）
- **內容**：先用一句摘要，再用 `##` 分節（基本資訊、交通、門票、建議時間、必看重點）
- **交叉引用**：相關實體用 `[[Wikilinks]]` 連結，一律使用 **Obsidian 短路徑**（如 `[[心齋橋]]`、`[[大阪城]]`），**不加路徑前綴**（禁止 `[[wiki/entities/區域/心齋橋]]`）
  - **例外（同名衝突）**：若目標與 `原始資料/` 等其他資料夾存在同名檔（如 `通天閣`、`日本環球影城`），短連結會解析歧義，此時**允許全路徑＋顯示別名**消歧義（如 `[[wiki/entities/景點/通天閣|通天閣]]`）；表格內的 `|` 須跳脫為 `\|`
- **來源**：最後用 `## 來源` 列出原始資料的 wikilink

## 📝 特別紀錄

- **查詢結果回覆**：不使用表格，盡量以條列式呈現

## ✍️ 寫作與格式規範

- **繁體中文**，精簡條列，不講廢話
- **語法**：內部連結用 `[[Wikilinks]]`（**短路徑，不加資料夾前綴**），外部用標準 `[Link](URL)`
- **檔案操作**：優先編輯既有檔案，不建立無意義的 README 或臨時檔
- 若工具或平台不支援某些 Obsidian 語法，仍應盡量保留 Markdown 與 wikilink 相容性

## 🛡️ 互動與安全

- **防呆確認**：需求不明確時，先問 1 個關鍵問題，絕不自行大量假設
- **隱私安全**：不保存、不上傳、不擴散敏感個資與憑證
- **唯讀保護**：絕不修改 `原始資料/` 下的檔案

## 📂 已確認行程

- `Osaka Trip/` 存放已確認的行程筆記（如機加酒、飯店訂單等）

### 🔄 消化快取機制

- AI 在 `wiki/log.md` 記錄每次消化 `Osaka Trip/` 的時間戳與檔案列表
- **觸發消化的條件**（以下任一成立才重新消化）：
  1. `Osaka Trip/` 內有新增或修改的檔案（比對 `updated` 欄位或檔案日期）
  2. 使用者明確下達「重新消化行程」指令
  3. 距上次消化超過 7 天
- 若以上條件均不成立，直接使用 wiki 現有知識回覆，跳過消化步驟

### 🗓 行程起草規則

觸發「起草每日行程」時，AI 應：

1. 讀取 `Osaka Trip/` 所有已確認資訊（機票時段、住宿地點、待辦清單）
2. 讀取 `wiki/concepts/` 中的規劃頁（行程規劃、美食指南、購物指南）
3. **參考** `原始資料/別人行程/` 中的他人攻略（如晉德的大阪行、購物4大商圈攻略）
4. 依以下框架生成 D1–D5 草稿，存入 `Osaka Trip/itinerary-draft.md`：
   - **D1（抵達日）**：機場 → 飯店入住 → 周邊輕鬆逛
   - **D2–D4（完整日）**：上午 / 下午 / 晚上各一主題，含預計交通與時長
   - **D5（返國日）**：退房 → 機場緩衝時間
5. 每格標注「已確認 ✅」或「待定 📌」，方便後續填補
6. 行程草稿不覆蓋已標注「已確認 ✅」的內容
7. 引用別人行程時，必須標注來源（如「參考：晉德的大阪行」）
